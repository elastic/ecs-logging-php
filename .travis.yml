language: php

sudo: false
dist: trusty

php:
  - 7.2
  - 7.3
  - 7.4snapshot

cache:
  directories:
    - $HOME/.composer/cache

matrix:
    fast_finish: true
    allow_failures:
      - php: 7.4snapshot

before_script:
  - composer install

script: composer test
